Coming later
  /who-has MODULE
  /mini-rank RANK (grants role, ask to share with others)
  /inventory ITEM
  /click-request MODULE (fakeDescription for ambush modules)
  /harvest (no tear down)

Discord events
- when a new user joins, DM them about:
  - /help, /login, /who-has, /who-is, /what-is, and /random-user.
- when a user signs into the bot, DM them:
  - confirm sign in
  - prompt them to use /befriend, /mailbox, and /mini-rank
  - inform them about ranking up + badges, and showing up in /who-is posts
  - subscribe buttons

TODO: Implement `DELETE /api/users/username/block`
